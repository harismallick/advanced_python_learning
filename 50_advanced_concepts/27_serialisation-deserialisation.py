# serialisation is the process of converting objects from a programming language 
# to a binary or text file, which can be transferred to other programs/services.
# The other programs would then deserialise the contents into their format.
# JSON is the most popular format for doing this will text.
# If you wish to do serialisation in binary, then use Protobuf (language agnostic)
# Python's built-in pickle files are not safe and should be used sparingly.