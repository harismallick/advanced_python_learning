# The heapq module in Python uses the heap queue algorithm, also known at the
# priority queue algorithm.
# In this binary tree structure, the parent node is always smaller (min-heap)
# or larger (max-heap) than the child nodes.
# By default, instantiating a heapq creates a min-heap.

